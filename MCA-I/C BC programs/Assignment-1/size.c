#include <stdio.h>
void main() {
  int a;
  float b;
  char c;
  double d;
  printf("\nsize of int %li:", sizeof(a));
  printf("\nsize of float %li:", sizeof(b));
  printf("\nsize of char %li:", sizeof(c));
  printf("\nsize of double %li:", sizeof(d));
}